import React,{useEffect,useState} from 'react'
const API_BASE=import.meta.env.VITE_API_BASE
export default function Leaderboard(){const[items,setItems]=useState<any[]>([]);useEffect(()=>{fetch(`${API_BASE}/api/leaderboard?limit=100`).then(r=>r.json()).then(setItems).catch(()=>{})},[]);return(<div><h2>Season Leaderboard</h2><table style={{width:'100%',borderCollapse:'collapse'}}><thead><tr><th style={{textAlign:'left',padding:8}}>#</th><th style={{textAlign:'left',padding:8}}>Nickname</th><th style={{textAlign:'right',padding:8}}>Score</th></tr></thead><tbody>{items.map((r,i)=>(<tr key={i} style={{borderTop:'1px solid rgba(255,255,255,.06)'}}><td style={{padding:8}}>{i+1}</td><td style={{padding:8}}>{r.nickname}</td><td style={{padding:8,textAlign:'right'}}>{r.score}</td></tr>))}</tbody></table></div>)}
